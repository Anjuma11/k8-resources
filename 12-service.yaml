apiVersion: v1
kind: Service
metadata:
  name: nginx
  labels:
    purpose: service-demo
spec:
  selectors:
    purpose: service-demo
    project: roboshop
    environment: dev
  ports:
    port: 80
    protocol: tcp
    target-port: 80


apiVersion: v1
kind: Pod
metadata:
  name: nginx
  labels:                                                                                                                                                                                                      
    purpose: service-demo
    project: roboshop
    environment: dev
spec:
  containers:
  - name: nginx
    image: nginx